import { Typography } from "@material-ui/core";
import React, { FC } from "react";

const GameResult : FC<{}> = () => {
    return (
        <Typography variant="h5" align="center">Finished</Typography>
    ); 
}

export default GameResult;